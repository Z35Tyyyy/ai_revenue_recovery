# --- Runtime dependencies only (dev/test tools live in requirements-dev.txt) ---

# Core
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
numpy>=1.26,<2.2
pandas>=2.2,<3
# Pinned exactly: the committed *.joblib models are unpickled at runtime and are
# coupled to the scikit-learn that trained them. A floating range risks
# InconsistentVersionWarning / load failures on a fresh install.
scikit-learn==1.9.0

# razorpay 1.4.x imports pkg_resources at load; Python 3.12+ no longer bundles it
# and setuptools>=81 removed it — pin below 81 so `import razorpay` works.
setuptools<81

# API
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-multipart>=0.0.9

# Razorpay + LLM (optional at runtime; graceful fallbacks if absent)
razorpay>=1.4,<2
anthropic>=0.39,<1          # LLM_PROVIDER=anthropic
openai>=1.40,<2             # LLM_PROVIDER=groq (OpenAI-compatible) or openai
httpx>=0.27,<1

# Utilities
python-dotenv>=1.0,<2
rich>=13.7,<14
