# --- Core runtime ---
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
numpy>=1.26,<2.2
pandas>=2.2,<3
scikit-learn>=1.4,<2

# --- API ---
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-multipart>=0.0.9

# --- Razorpay + LLM (optional at runtime; graceful fallbacks if absent) ---
razorpay>=1.4,<2
anthropic>=0.39,<1          # LLM_PROVIDER=anthropic
openai>=1.40,<2             # LLM_PROVIDER=groq (OpenAI-compatible) or openai
httpx>=0.27,<1

# --- Utilities ---
python-dotenv>=1.0,<2
rich>=13.7,<14
typer>=0.12,<1

# --- Dev / test ---
pytest>=8.1,<9
pytest-cov>=5.0,<6
ruff>=0.4,<1
